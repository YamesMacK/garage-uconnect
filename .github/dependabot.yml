version: 2
updates:
  # Keeps the SHA-pinned actions in .github/workflows current.
  - package-ecosystem: github-actions
    directory: "/"
    schedule:
      interval: weekly
  # Bump PRs for the exact-pinned py-uconnect.
  - package-ecosystem: pip
    directory: "/"
    schedule:
      interval: weekly
